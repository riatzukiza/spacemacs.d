;; -*- mode: emacs-lisp; lexical-binding: t -*-
;;; core/user-init.el --- Pre-package user initialization  -*- lexical-binding: t -*-
;;; Commentary:
;; This file holds the body of `dotspacemacs/user-init'.
;; Use it for variables that must be set before packages are loaded.
;; Currently there is no early initialization required; the file is kept as a
;; stable hook point for future changes.

;;; Code:

;;; user-init.el ends here
