;;; packages.el --- unique-files layer -*- lexical-binding: t; -*-
;;; Commentary:
;; Spacemacs layer for timestamped "unique" files per major mode.

;;; Code:

(defconst unique-files-packages '())

;;; packages.el ends here
