# GIT_MODULE_INDEX.md — .spacemacs.d (knoxx)

Position of this repository in the federation on knoxx.

## Parent

| field  | value                                                            |
|--------|------------------------------------------------------------------|
| path   | `/home/err` (federation superproject)                            |
| remote | (superproject-local federation; see superproject's own index)     |
| branch | `device/knoxx`                                                    |

This repository is checked out at `/home/err/.spacemacs.d` on the knoxx host
and sits under the `/home/err` federation superproject, which is also on
`device/knoxx`.

## Children

None. This repository contains no nested git repositories: no
`layers/*/` directory carries a `.git` entry (verified by inspection — the
only `.git` under the repo root is the top-level one).

| path | repo/remote | branch | role | one-line |
|------|-------------|--------|------|----------|
| —    | —           | —      | —    | no nested repos |
